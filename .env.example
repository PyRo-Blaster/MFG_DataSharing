MFG_USERNAME=change-me
MFG_PASSWORD=change-me
# Optional: stable session signing key so logins survive restarts / multiple workers.
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
MFG_SECRET_KEY=change-me
